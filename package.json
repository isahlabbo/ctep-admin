{
  "name": "exam-suite",
  "version": "1.0.0",
  "description": "Offline exam client/admin (Electron)",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron .",
    "dist": "electron-builder --publish never"
  },
  "build": {
    "appId": "ctep.catsol.ng.exam-suite",
    "productName": "CTep Admin Suite",
    "files": [
      "src/**/*",
      "assets/**/*",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "assets/",
        "to": "assets",
        "filter": [
          "**/*"
        ]
      }
    ],
    "win": {
      "target": ["nsis"]
    },
    "linux": {
      "target": ["AppImage"]
    },
    "mac": {
      "target": ["dmg"]
    }
  },
  "dependencies": {
    "sqlite3": "^5.1.7",
    "electron-is-dev": "^2.0.0",
    "bcryptjs": "^3.0.3"
  },
  "devDependencies": {
    "electron": "^29.0.0",
    "electron-builder": "^24.6.0"
  }
}
